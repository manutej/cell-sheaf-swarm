# Adversarial read — insight, not a lab bench

Four seats, this volume, before the change. Findings are about the picture, not the schema.

| seat | sev | claim | evidence |
| --- | --- | --- | --- |
| user | P0 | I cannot tell what to do | Header is `ok ρ` / `hurt ρ` / `JEV n/128`. No sentence names a folder. |
| operator | P0 | I cannot act on a map | Inspector leads with kind and dim. Blockers are unsorted. Tapping does not isolate one claim. |
| craft | P1 | Legal glue shouts as loud as a break | Every curve is ~the same weight. Trunk view is the only place green is the path, and it is not the default. |
| red | P0 | The views are a costume of the paper | Renaming the chip (pillars, ρ, strata, harmonic) does not change the question. Strip the math words and nothing useful remains. |

## What changed (same tokens, same contract)

Do not restyle `template/tokens.css`. Color is still glue. Gold is still earned.

1. The title is a **verdict** computed from the sheaf: how many folders may fold, and the first two blockers in words.
2. Default sheet is **Look here**, ranked broken → missing → strange. A row focuses that map.
3. **Ok curves recede.** Broken, missing, and strange stay heavy and are labeled (`won't fold`, `no map`, `needs a person`).
4. A selected folder or map **dims the rest** to its neighborhood.
5. Tab words are questions (`Moving`, `May fold`, `In flight`, `Agrees`, `Wrong`, `Lives`), not instrument names.

JSON stays the sheaf. A new contract gets the same reading with no chrome fork.
